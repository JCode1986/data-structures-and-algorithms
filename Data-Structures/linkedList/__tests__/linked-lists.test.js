'use strict';

jest.mock('fs');

const linkedList = require('../linked-list');

/* Can successfully instantiate an empty linked list
Can properly insert into the linked list
The head property will properly point to the first node in the linked list
Can properly insert multiple nodes into the linked list
Will return true when finding a value within the linked list that exists
Will return false when searching for a value in the linked list that does not exist
Can properly return a collection of all the values that exist in the linked list */


describe('Linked Lists', () => {
  it('Can successfully instantiate an empty linked list', () => {
    // let node = new Node('foo');
    // expect(node).toBe(true);
  })

  it('append (value) should append node to end of linked list', () => {
    
  })

  it('return null if number is greater than length of list', () => {
    // let node = linkedList.insert('foo');
    // console.log(node)
    // let number = linkedList.kthFromEnd(3)
    // expect(number).toBe(null);
  })

});