'use strict';

let array = [8, 7, 6, 5, 4, 3, 2, 1];

function selectionSort(arr) {
    let temp = 0;
    let min = arr[0];
    for(let i = 0; i < arr.length - 1; i++) {
        if(arr[i + 1] < min) {
            
        }
    }
}